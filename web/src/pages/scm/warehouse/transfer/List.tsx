import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function TransferList() {
  return (
    <Card>
      <Title level={4}>调拨管理</Title>
      <p>调拨管理功能开发中，敬请期待...</p>
    </Card>
  );
}
