apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{deployment_name}}
  namespace: {{namespace}}
  labels:
    app: {{deployment_name}}
    device-group: {{group_id}}
    app.kubernetes.io/managed-by: ithings
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{deployment_name}}
      device-group: {{group_id}}
      app.kubernetes.io/managed-by: ithings
  template:
    metadata:
      labels:
        app: {{deployment_name}}
        device-group: {{group_id}}
        app.kubernetes.io/managed-by: ithings
    spec:
      containers:
{{#each device_containers}}
      - name: {{this.name}}
        image: {{this.image}}
        imagePullPolicy: Always
        args: ["--configfile", "/config/config.json", "--group"]
        volumeMounts:
        - name: device-config
          mountPath: /config
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
{{/each}}
      - name: driver
        image: {{driver_image}}
        imagePullPolicy: Always
        args: ["--configfile", "/config/config.json"]
        volumeMounts:
        - name: device-config
          mountPath: /config
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
      - name: zmq-router
        image: {{router_image}}
        imagePullPolicy: Always
        args: ["--frontend", "tcp://*:5550", "--backend", "tcp://*:5551", "--loglevel", "info"]
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 200m
            memory: 128Mi
{{#if node_selector}}
      nodeSelector:
{{#each node_selector}}
        {{key}}: {{value}}
{{/each}}
{{/if}}
      volumes:
      - name: device-config
        configMap:
          name: {{configmap_name}}
      restartPolicy: Always
