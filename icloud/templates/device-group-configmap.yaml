apiVersion: v1
kind: ConfigMap
metadata:
  name: {{configmap_name}}
  namespace: {{namespace}}
  labels:
    device-group: {{group_id}}
    app.kubernetes.io/managed-by: ithings
data:
{{#each instances}}
  {{filename}}: |-
    {{{value}}}
{{/each}}